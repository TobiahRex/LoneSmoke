/* eslint-disable no-console */

require('dotenv').load({ silent: true });

export default (type) =>
new Promise((resolve, reject) => {
  const typeKeys = Object.keys(type);
  
  // if type === "Bounce"
  if (typeKeys.includes('')) {

  }
});
